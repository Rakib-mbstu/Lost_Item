# Google OAuth Client ID (same value used in backend appsettings.json → Google:ClientId)
VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_OAUTH_CLIENT_ID.apps.googleusercontent.com

# Backend base URL (only needed if not using the Vite dev proxy)
VITE_API_URL=http://localhost:5099
